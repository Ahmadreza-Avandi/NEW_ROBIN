# 🚀 راهنمای کامل نصب و راه‌اندازی WordPress CRM Integration برای Rabin

## ✅ وضعیت فعلی سیستم

سیستم کاملاً آماده و تست شده است:
- ✅ افزونه WordPress فارسی (119.11 KB)
- ✅ Multi-tenant CRM فعال
- ✅ Tenant "rabin" با 2 مشتری تست
- ✅ کلید API اختصاصی فعال
- ✅ Admin panel مخصوص rabin

---

## 📦 مرحله 1: نصب افزونه در WordPress

### فایل افزونه:
```
مسیر: /mnt/d/NEW_ROBIN/wordpress-crm-integration/build/wordpress-crm-integration-v1.0.0.zip
حجم: 119.11 KB
```

### نحوه نصب:
1. وارد پنل مدیریت WordPress شوید
2. به **افزونه‌ها** → **افزونه جدید** بروید
3. روی **آپلود افزونه** کلیک کنید
4. فایل `wordpress-crm-integration-v1.0.0.zip` را انتخاب کنید
5. روی **نصب اکنون** کلیک کنید
6. پس از نصب، روی **فعال‌سازی افزونه** کلیک کنید

---

## ⚙️ مرحله 2: تنظیمات اتصال

پس از فعال‌سازی، منوی **CRM Integration** در پنل مدیریت ظاهر می‌شود.

### اطلاعات اتصال برای tenant rabin:

```
🔗 تنظیمات اتصال:
CRM URL: http://localhost:3000
API Key: wp_crm_rabin_06292d18f971552edbc0123cd914be5f65e4af00cadfdfa75c2e970f3a70357c
Tenant Key: rabin

📊 آدرس‌های مهم:
- Admin Panel Rabin: http://localhost:3000/rabin/admin-panel
- API Endpoint: http://localhost:3000/api/integrations/wordpress/customers
- تست اتصال: http://localhost:3000/api/integrations/wordpress/test
```

### مراحل تنظیم:
1. به **CRM Integration** → **تنظیمات عمومی** بروید
2. اطلاعات بالا را در فیلدهای مربوطه وارد کنید:
   - **آدرس CRM**: `http://localhost:3000`
   - **کلید API**: `wp_crm_rabin_06292d18f971552edbc0123cd914be5f65e4af00cadfdfa75c2e970f3a70357c`
   - **کلید Tenant**: `rabin`
3. روی **تست اتصال** کلیک کنید
4. اگر پیام موفقیت دریافت کردید، روی **ذخیره تنظیمات** کلیک کنید

---

## 🔄 مرحله 3: فعال‌سازی همگام‌سازی

### همگام‌سازی خودکار:
1. به تب **تنظیمات پیشرفته** بروید
2. گزینه‌های زیر را فعال کنید:
   - ✅ **همگام‌سازی کاربران جدید**
   - ✅ **همگام‌سازی سفارشات WooCommerce** (اگر دارید)
   - ✅ **همگام‌سازی به‌روزرسانی‌ها**
3. **فاصله همگام‌سازی**: 5 دقیقه (پیشنهادی)
4. روی **ذخیره** کلیک کنید

### نقشه‌برداری فیلدها:
1. به تب **نقشه‌برداری فیلدها** بروید
2. فیلدهای WordPress را به فیلدهای CRM متصل کنید:
   - **نام** ← `display_name`
   - **ایمیل** ← `user_email`
   - **تلفن** ← `billing_phone` (WooCommerce)
   - **شرکت** ← `billing_company` (WooCommerce)
   - **آدرس** ← `billing_address_1` (WooCommerce)
3. روی **ذخیره نقشه‌برداری** کلیک کنید

---

## 🧪 مرحله 4: تست عملکرد

### تست دستی:
1. یک کاربر جدید در WordPress ثبت‌نام کنید
2. به **CRM Integration** → **گزارش‌های همگام‌سازی** بروید
3. باید رکورد جدیدی از انتقال داده ببینید

### بررسی در CRM:
1. به admin panel rabin بروید: `http://localhost:3000/rabin/admin-panel`
2. در تب **مشتریان** باید کاربر جدید را ببینید
3. منبع آن باید **WordPress** باشد

---

## 📊 مرحله 5: نظارت و مدیریت

### Admin Panel Rabin:
- **آدرس**: http://localhost:3000/rabin/admin-panel
- **ویژگی‌ها**:
  - مشاهده تمام مشتریان tenant rabin
  - آمار مشتریان WordPress و دستی
  - اطلاعات اتصال WordPress

### گزارش‌گیری:
- **گزارش‌های همگام‌سازی**: در پنل WordPress
- **آمار مشتریان**: در admin panel rabin
- **وضعیت API**: در تنظیمات افزونه

---

## 🔧 عیب‌یابی

### مشکلات رایج:

#### 1. خطای اتصال:
```
✅ بررسی کنید: http://localhost:3000 در دسترس است
✅ کلید API صحیح وارد شده
✅ فایروال مشکلی ایجاد نمی‌کند
```

#### 2. داده منتقل نمی‌شود:
```
✅ همگام‌سازی فعال است
✅ نقشه‌برداری فیلدها درست تنظیم شده
✅ گزارش‌های خطا را بررسی کنید
```

#### 3. کاربران قدیمی منتقل نمی‌شوند:
```
✅ فقط کاربران جدید به‌طور خودکار منتقل می‌شوند
✅ برای انتقال کاربران قدیمی از ابزار "همگام‌سازی دسته‌ای" استفاده کنید
```

---

## 📋 چک‌لیست نهایی

- [ ] افزونه نصب و فعال شده
- [ ] اطلاعات اتصال وارد شده
- [ ] تست اتصال موفق بوده
- [ ] همگام‌سازی فعال شده
- [ ] نقشه‌برداری فیلدها تنظیم شده
- [ ] تست با کاربر جدید انجام شده
- [ ] admin panel rabin قابل دسترسی است

---

## 🎉 تبریک!

سیستم WordPress CRM Integration برای tenant rabin کاملاً آماده است!

### آمار فعلی:
- **Tenant rabin**: 2 مشتری (1 WordPress + 1 دستی)
- **کلید API**: فعال و آماده
- **Admin Panel**: http://localhost:3000/rabin/admin-panel

### ویژگی‌های فعال:
✅ همگام‌سازی خودکار کاربران جدید  
✅ پشتیبانی WooCommerce  
✅ جداسازی کامل داده‌ها  
✅ رابط کاربری فارسی  
✅ گزارش‌گیری جامع  
✅ مدیریت خطاها  

**سیستم آماده استفاده است! 🚀**