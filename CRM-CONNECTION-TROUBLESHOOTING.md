# راهنمای حل مشکل اتصال CRM

## مشکل فعلی
```
تست اتصال ✗ امکان اتصال به CRM وجود ندارد
GET / 404 in 81ms
```

## علت مشکل
پلاگین به آدرس اشتباه درخواست می‌فرستد. باید به endpoint صحیح متصل شود.

## راه حل

### 1. تنظیمات صحیح در WordPress

در پنل مدیریت WordPress > CRM Integration > تنظیمات:

```
آدرس CRM: http://localhost:3000
کلید API: [کلید دریافتی از پنل مدیریت]
کلید Tenant: [شناسه tenant شما]
```

**نکته مهم**: آدرس CRM نباید شامل `/api` باشد چون پلاگین خودش آن را اضافه می‌کند.

### 2. ایجاد کلید API

1. به آدرس `http://localhost:3000/secret-zone-789/admin-panel` بروید
2. وارد بخش "کلیدهای API" شوید
3. کلید جدید برای WordPress ایجاد کنید
4. کلید را کپی کنید

### 3. تست اتصال

پس از وارد کردن تنظیمات، روی "تست اتصال" کلیک کنید. باید پیام موفقیت دریافت کنید.

## API Endpoints موجود

پلاگین به این endpoint ها متصل می‌شود:

```
GET  /api/integrations/wordpress/test        # تست اتصال
POST /api/integrations/wordpress/customers   # ارسال مشتری
POST /api/integrations/wordpress/products    # ارسال محصول  
POST /api/integrations/wordpress/orders      # ارسال سفارش
```

## ویژگی‌های جدید اضافه شده

### 1. نقشه‌برداری فیلدها
- **مسیر**: WordPress Admin > CRM Integration > نقشه‌برداری فیلدها
- **قابلیت**: تعیین اینکه هر فیلد WordPress با کدام فیلد CRM مطابقت کند

### 2. همگام‌سازی خودکار
- **مشتریان جدید**: هنگام ثبت‌نام کاربر جدید
- **محصولات جدید**: هنگام ایجاد/ویرایش محصول
- **سفارشات جدید**: هنگام ثبت سفارش جدید

### 3. همگام‌سازی دستی
- **مسیر**: WordPress Admin > CRM Integration > همگام‌سازی دستی
- **قابلیت**: همگام‌سازی دسته‌ای تمام داده‌ها

### 4. صف همگام‌سازی
- پردازش ناهمزمان داده‌ها
- تلاش مجدد در صورت خطا
- لاگ کامل عملیات

## نحوه استفاده

### مرحله 1: نصب و فعال‌سازی
1. فایل ZIP جدید را دانلود کنید
2. پلاگین قبلی را غیرفعال کنید
3. پلاگین جدید را آپلود و فعال کنید

### مرحله 2: تنظیمات اولیه
1. **تنظیمات پایه**: آدرس CRM، کلید API، کلید Tenant
2. **تست اتصال**: اطمینان از اتصال صحیح
3. **فعال‌سازی همگام‌سازی**: تیک "فعال‌سازی همگام‌سازی"

### مرحله 3: نقشه‌برداری فیلدها
1. برو به "نقشه‌برداری فیلدها"
2. برای هر فیلد CRM، نام فیلد WordPress را وارد کن
3. مثال:
   ```
   first_name -> first_name
   last_name -> last_name  
   email -> user_email
   phone -> billing_phone
   ```

### مرحله 4: همگام‌سازی اولیه
1. برو به "همگام‌سازی دستی"
2. همگام‌سازی مشتریان موجود
3. همگام‌سازی محصولات موجود (اگر WooCommerce فعال است)
4. همگام‌سازی سفارشات موجود

## مثال تنظیمات

### نقشه‌برداری مشتریان
```
first_name: first_name
last_name: last_name
email: user_email
phone: billing_phone
company_name: billing_company
address: billing_address_1
city: billing_city
state: billing_state
country: billing_country
postal_code: billing_postcode
```

### نقشه‌برداری محصولات
```
name: name
description: description
price: price
sku: sku
stock_quantity: stock_quantity
category: category_names
```

### نقشه‌برداری سفارشات
```
order_number: order_number
status: status
total: total
currency: currency
payment_method: payment_method
customer_email: billing_email
customer_phone: billing_phone
```

## عیب‌یابی

### خطای 404
- بررسی کنید آدرس CRM صحیح باشد
- مطمئن شوید سرور CRM در حال اجرا است

### خطای 401 (Unauthorized)
- کلید API را بررسی کنید
- مطمئن شوید کلید فعال است

### خطای 500
- لاگ‌های سرور CRM را بررسی کنید
- فرمت داده‌های ارسالی را بررسی کنید

### مشاهده لاگ‌ها
- **WordPress**: WordPress Admin > CRM Integration > لاگ‌ها
- **سرور**: لاگ‌های terminal سرور CRM

## پشتیبانی

اگر مشکلی داشتید:
1. لاگ‌های WordPress و سرور را بررسی کنید
2. تنظیمات را دوباره بررسی کنید
3. تست اتصال را انجام دهید
4. در صورت نیاز، پلاگین را غیرفعال و مجدداً فعال کنید

---

**نسخه جدید پلاگین آماده است و تمام مشکلات برطرف شده!** 🚀