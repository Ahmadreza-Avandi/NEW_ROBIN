# ✅ سیستم کامل مدیریت تصاویر محصولات

## 🎯 ویژگی‌های پیاده‌سازی شده

### 1️⃣ دیتابیس
- ✅ فیلد `image` به جدول `products` اضافه شد
- ✅ اسکریپت خودکار برای اضافه کردن فیلد: `add-product-image.cjs`

### 2️⃣ API Endpoints

#### آپلود تصویر
- **مسیر**: `POST /api/products/upload-image`
- **ویژگی‌ها**:
  - پشتیبانی از JPG, PNG, WEBP, GIF
  - محدودیت حجم: 5MB
  - نام فایل یونیک با UUID
  - ذخیره در: `public/uploads/products/`

#### دریافت لیست محصولات
- **مسیر**: `GET /api/tenant/products`
- **فیلترها**: search, category, status
- **شامل**: فیلد image

#### دریافت یک محصول
- **مسیر**: `GET /api/tenant/products/[id]`
- **شامل**: تمام اطلاعات محصول + تصویر

#### ایجاد محصول
- **مسیر**: `POST /api/tenant/products`
- **فیلد جدید**: image (اختیاری)

#### بروزرسانی محصول
- **مسیر**: `PUT /api/tenant/products`
- **قابلیت**: آپدیت تصویر محصول

### 3️⃣ صفحات Frontend

#### لیست محصولات
- **مسیر**: `/[tenant_key]/dashboard/products`
- ✅ نمایش تصویر در کارت محصولات
- ✅ فیلتر کامل (جستجو، دسته‌بندی، وضعیت)
- ✅ fallback برای محصولات بدون تصویر

#### افزودن محصول
- **مسیر**: `/[tenant_key]/dashboard/products/new`
- ✅ آپلود تصویر با انتخاب فایل
- ✅ پیش‌نمایش تصویر قبل از ذخیره
- ✅ امکان حذف و تغییر تصویر
- ✅ اعتبارسنجی نوع و حجم فایل

#### جزئیات محصول
- **مسیر**: `/[tenant_key]/dashboard/products/[id]`
- ✅ نمایش تصویر محصول در بالای صفحه
- ✅ نمایش تمام اطلاعات محصول
- ✅ دکمه ویرایش

#### ویرایش محصول
- **مسیر**: `/[tenant_key]/dashboard/products/[id]/edit`
- ✅ نمایش تصویر فعلی محصول
- ✅ آپلود تصویر جدید
- ✅ حذف تصویر
- ✅ تغییر تصویر
- ✅ ذخیره تمام تغییرات

## 🚀 نحوه استفاده

### مرحله 1: اضافه کردن فیلد به دیتابیس
```bash
node add-product-image.cjs
```

### مرحله 2: ریستارت سرور
```bash
npm run dev
```

### مرحله 3: تست عملکرد

1. **افزودن محصول با تصویر**:
   - به `/rabin/dashboard/products` بروید
   - روی "افزودن محصول" کلیک کنید
   - تصویر را انتخاب کنید
   - فرم را تکمیل و ذخیره کنید

2. **مشاهده محصول**:
   - روی کارت محصول کلیک کنید
   - تصویر در بالای صفحه نمایش داده می‌شود

3. **ویرایش تصویر**:
   - روی "ویرایش" کلیک کنید
   - تصویر فعلی نمایش داده می‌شود
   - می‌توانید تصویر جدید آپلود کنید
   - یا تصویر فعلی را حذف کنید

## 📁 ساختار فایل‌ها

```
app/
├── api/
│   ├── products/
│   │   └── upload-image/
│   │       └── route.ts          # API آپلود تصویر
│   └── tenant/
│       └── products/
│           ├── route.ts           # لیست و ایجاد محصولات
│           └── [id]/
│               └── route.ts       # دریافت و بروزرسانی محصول
│
└── [tenant_key]/
    └── dashboard/
        └── products/
            ├── page.tsx           # لیست محصولات
            ├── new/
            │   └── page.tsx       # افزودن محصول
            └── [id]/
                ├── page.tsx       # جزئیات محصول
                └── edit/
                    └── page.tsx   # ویرایش محصول

public/
└── uploads/
    └── products/                  # تصاویر محصولات
```

## 🔒 امنیت

✅ بررسی نوع فایل (فقط تصاویر)
✅ محدودیت حجم (5MB)
✅ نام فایل یونیک (UUID)
✅ احراز هویت برای آپلود
✅ بررسی tenant_key

## 📊 مثال استفاده از API

### آپلود تصویر
```javascript
const formData = new FormData();
formData.append('image', imageFile);

const response = await fetch('/api/products/upload-image', {
  method: 'POST',
  body: formData,
});

// Response: { success: true, data: { url: '/uploads/products/...', filename: '...' } }
```

### ایجاد محصول با تصویر
```javascript
await fetch('/api/tenant/products', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'X-Tenant-Key': 'rabin'
  },
  body: JSON.stringify({
    name: 'محصول نمونه',
    description: 'توضیحات',
    image: '/uploads/products/abc123.jpg',
    category: 'ماشین آلات',
    price: 100000,
    currency: 'IRR',
    status: 'active'
  }),
});
```

### بروزرسانی تصویر محصول
```javascript
await fetch('/api/tenant/products', {
  method: 'PUT',
  headers: {
    'Content-Type': 'application/json',
    'X-Tenant-Key': 'rabin'
  },
  body: JSON.stringify({
    id: 'product-id',
    image: '/uploads/products/new-image.jpg'
  }),
});
```

## ✨ ویژگی‌های UI/UX

- 🎨 پیش‌نمایش زنده تصویر
- 🖼️ نمایش تصویر در کارت‌های محصول
- 📸 تصویر بزرگ در صفحه جزئیات
- ✏️ ویرایش آسان تصویر
- 🗑️ حذف تصویر با یک کلیک
- ⚡ آپلود سریع و بدون رفرش صفحه
- 📱 طراحی ریسپانسیو

## 🎉 تمام!

همه چیز آماده است. فقط کافیه:
1. `node add-product-image.cjs` رو اجرا کنی
2. سرور رو ریستارت کنی
3. از قابلیت‌های جدید لذت ببری!
