# ⚡ حل سریع مشکل لاگین

## 🎯 مشکل شما:
رمز عبور در دیتابیس هش نشده است!

## ✅ راه‌حل سریع (1 دقیقه):

### مرحله 1: باز کردن phpMyAdmin
```
http://localhost/phpmyadmin
```

### مرحله 2: انتخاب دیتابیس
- روی `school` کلیک کنید

### مرحله 3: رفتن به تب SQL
- روی تب "SQL" کلیک کنید

### مرحله 4: کپی و اجرای این کد:

```sql
DELETE FROM user WHERE nationalCode IN ('0000000000', '1');

INSERT INTO user (id, fullName, nationalCode, phoneNumber, password, roleId) 
VALUES (1, 'مدیر سیستم', '0000000000', '09000000000', 
'$2a$10$j/KCE2ssT13HxS505UR7HecVmy53oekUg.2k5/8omY6CDoFEvYgU2', 1);
```

### مرحله 5: کلیک روی "Go"

---

## 🔐 حالا لاگین کنید:

```
آدرس: http://localhost:3000/login

کد ملی: 0000000000
رمز عبور: admin123
```

---

## ✅ تست کنید:

1. صفحه لاگین را باز کنید
2. اطلاعات بالا را وارد کنید
3. روی "ورود به حساب" کلیک کنید
4. باید به داشبورد منتقل شوید

---

## 🎉 بعد از لاگین موفق:

از منوی سمت راست می‌توانید:

### مدیریت معلمان:
- ✅ افزودن معلم (مدیر)
- ✅ لیست معلمان

### درس/رشته/کلاس:
- ✅ مدیریت جامع (مدیر)
- ✅ ایجاد کلاس
- ✅ ایجاد درس
- ✅ ایجاد رشته/پایه
- ✅ مشاهده پایه‌ها (مدیر)

### حاضرین/غایبین:
- ✅ مشاهده حاضری‌ها
- ✅ مشاهده غایبین
- ✅ آخرین لحظه

---

## ❌ اگر باز هم خطا داد:

### بررسی 1: دیتابیس درست است؟
```sql
SELECT DATABASE();
-- باید school باشد
```

### بررسی 2: کاربر وجود دارد؟
```sql
SELECT * FROM user WHERE nationalCode = '0000000000';
-- باید 1 رکورد برگرداند
```

### بررسی 3: رمز هش شده است؟
```sql
SELECT LEFT(password, 10) FROM user WHERE nationalCode = '0000000000';
-- باید $2a$10$ باشد
```

---

## 🔧 اگر همچنان مشکل دارید:

1. Next.js را restart کنید:
   ```bash
   # Ctrl+C برای توقف
   npm run dev
   ```

2. Cache مرورگر را پاک کنید (Ctrl+Shift+Delete)

3. Console مرورگر را باز کنید (F12) و خطاها را بررسی کنید

---

## 📞 نکته مهم:

این رمز فقط برای تست است. بعد از لاگین حتماً:
1. به پروفایل بروید
2. رمز عبور را تغییر دهید
3. از رمز قوی استفاده کنید

---

تمام! حالا باید بتوانید لاگین کنید 🚀
