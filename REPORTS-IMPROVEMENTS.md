# بهبودهای صفحه گزارش‌ها

## ✅ تغییرات اعمال شده

### 1. رفع مشکل دسترسی مدیر عامل
**مشکل قبلی**: فقط نقش `ceo` میتونست گزارش‌ها رو ببینه

**راه‌حل**: 
- لیست کامل نقش‌های مدیریتی اضافه شد:
  - ceo, مدیر, مدیر عامل, مدیرعامل
  - sales_manager, مدیر فروش
  - admin, ادمین
  - manager, مدیر کل
  - supervisor, سرپرست
  - team_lead, سرگروه

### 2. تغییر واژه "کارمند" به "همکار"
- در تمام بخش‌های رابط کاربری
- در فیلترها
- در آمارها

### 3. فیلتر بازه تاریخی
**قبل**: فقط یک تاریخ میشد انتخاب کرد

**بعد**: 
- فیلتر "از تاریخ" (تاریخ شروع)
- فیلتر "تا تاریخ" (تاریخ پایان)
- امکان مشاهده گزارش‌ها در یک بازه زمانی

### 4. بهبود API
- پشتیبانی از پارامترهای `start_date` و `end_date`
- کوئری دیتابیس با `>=` و `<=` برای بازه تاریخی
- لاگ‌های بهتر برای دیباگ

## 📊 نحوه استفاده

### فیلتر بازه تاریخی
1. "از تاریخ" را انتخاب کنید (مثلاً 1403/09/01)
2. "تا تاریخ" را انتخاب کنید (مثلاً 1403/09/15)
3. گزارش‌های بین این دو تاریخ نمایش داده می‌شود

### فیلتر همکار
- "همه همکاران" را انتخاب کنید برای مشاهده همه
- یا یک همکار خاص را انتخاب کنید

### جستجو
- در محتوای گزارش‌ها جستجو کنید
- در نام همکاران جستجو کنید

## 🔧 تغییرات فنی

### Frontend (`app/[tenant_key]/dashboard/reports/page.tsx`)
```typescript
// قبل
const [selectedDate, setSelectedDate] = useState('');

// بعد
const [startDate, setStartDate] = useState('');
const [endDate, setEndDate] = useState('');
```

### Backend (`app/api/reports/route.ts`)
```typescript
// قبل
if (date) {
    whereClause += ' AND dr.report_date = ?';
    params.push(date);
}

// بعد
if (start_date) {
    whereClause += ' AND dr.report_date >= ?';
    params.push(start_date);
}

if (end_date) {
    whereClause += ' AND dr.report_date <= ?';
    params.push(end_date);
}
```

## 🎯 مثال‌های استفاده

### مشاهده گزارش‌های یک هفته
- از تاریخ: 1403/09/08
- تا تاریخ: 1403/09/15

### مشاهده گزارش‌های یک ماه
- از تاریخ: 1403/09/01
- تا تاریخ: 1403/09/30

### مشاهده گزارش‌های یک همکار در یک بازه
- از تاریخ: 1403/09/01
- تا تاریخ: 1403/09/15
- همکار: علی احمدی

## ✨ ویژگی‌های اضافی

- ✅ پاک کردن تمام فیلترها با یک کلیک
- ✅ جستجوی زنده در گزارش‌ها
- ✅ نمایش آمار در بالای صفحه
- ✅ طراحی ریسپانسیو
- ✅ لاگ‌های کامل برای دیباگ

## 🚀 تست

برای تست تغییرات:
1. سرور را ریستارت کنید
2. به `/rabin/dashboard/reports` بروید
3. با نقش مدیر عامل وارد شوید
4. فیلترهای تاریخی را امتحان کنید
5. گزارش‌ها باید نمایش داده شوند

## 🐛 رفع مشکلات

اگر همچنان مشکل دسترسی دارید:
1. کنسول مرورگر را باز کنید (F12)
2. به تب Console بروید
3. لاگ‌های زیر را چک کنید:
   - `📊 Current user in reports`
   - `👤 Current user role`
   - `✅ Is manager`
   - `📊 Reports API - User role`
   - `✅ Reports API - Is Manager`

اگر `Is manager: false` بود، نقش کاربر در لیست نقش‌های مدیریتی نیست.
